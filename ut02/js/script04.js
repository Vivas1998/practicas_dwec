const pi = 3.1415;
let radio = parseInt(prompt("Indica el radio del circulo"));
let perimetro = 2 * pi * radio;
let area = pi * radio * radio;

console.log("El perímetro del círculo de radio " + radio + " es " + perimetro + " y su área es " + area);

